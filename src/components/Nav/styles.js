import styled from "styled-components";

export const Container = styled.nav`
  position: relative;

  ul {
    padding: 1rem 0;
    text-align: center;
    display: flex;
    justify-content: center;
    gap: 1.25rem;
  }

  li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  img {
    width: 1.5rem;
  }
`
